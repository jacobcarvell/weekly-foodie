export default (url, title, image, ingredients, method) => {
    return {
        url: url,
        title: title,
        image: image,
        ingredients: ingredients,
        method: method  
    }
  }